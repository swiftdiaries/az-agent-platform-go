# Copy into the environment used by `docker compose config` and `docker compose up`.
# Leave no production credentials in this file. Every empty value is required at runtime.
DATABASE_URL=
POSTGRES_PASSWORD=
TEST_JAVA_MCP_ENDPOINT=
KEYCLOAK_JWT_ISSUER=
KEYCLOAK_JWKS_URL=
KEYCLOAK_JWT_AUDIENCE=
KEYCLOAK_JWT_SOURCE=authorization_bearer
KEYCLOAK_JWT_HEADER=Authorization
KEYCLOAK_USER_CLAIM_PATH=
KEYCLOAK_TENANT_CLAIM_PATH=
KEYCLOAK_JWT_ALLOWED_ALGORITHMS=RS256
AZ_AGENT_FOUNDRY_PROJECT_ENDPOINT=
AZ_AGENT_FOUNDRY_DEPLOYMENT=
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
OTEL_SERVICE_NAME=az-agent-platform
OTEL_SERVICE_VERSION=
OTEL_EXPORTER_OTLP_ENDPOINT=
OTEL_EXPORTER_OTLP_HEADERS=
